# Wait for the VM to start 
echo "Waiting for qemu  to open port ${port}..."
if wait_until_port_used "${host}:${port}" 300; then
  echo "Discovered websockify listening on port ${port}!"
else
  echo "Timed out waiting for the compute node  to open port ${port}!"
  clean_up 1
fi
sleep 10

